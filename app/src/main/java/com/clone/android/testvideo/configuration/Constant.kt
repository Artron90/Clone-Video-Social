package com.clone.android.testvideo.configuration

object Constant {

    // set max number of video to cache
    const val NUMBER_CONTENT_CACHE = 2

    const val DEFAULT_MIN_BUFFER_MS = 500
    const val DEFAULT_MAX_BUFFER_MS = 3000
    const val DEFAULT_MIN_BUFFER_FOR_PLAY_BACK = 500
    const val DEFAULT_MIN_BUFFER_AFTER_RE_BUFFER = 500
}